local ui = {
    health    = require "ui/health",
    constants = require "ui/constants",
    palette   = require "ui/palette",
    menu      = require "ui/menu",
    number    = require "ui/number",
    scroll     = require "ui/scroll"
}

return ui
